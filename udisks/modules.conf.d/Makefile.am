## Process this file with automake to produce Makefile.in

moduleconfdir = $(sysconfdir)/udisks2/modules.conf.d


if ENABLE_DAEMON

if HAVE_LSM
moduleconf_DATA = udisks2_lsm.conf
endif # HAVE_LSM

endif # ENABLE_DAEMON
